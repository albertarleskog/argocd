apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data-prepper
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    path: apps/data-prepper/manifests
    targetRevision: HEAD
  destination:
    namespace: data-prepper
    server: {{ .Values.spec.destination.server }}
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
